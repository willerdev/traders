# Same-origin API path — Vite dev server proxies to VITE_PROXY_TARGET (no CORS).
VITE_API_URL=/api/v1
VITE_PROXY_TARGET=https://traders-c53s.onrender.com

# Local backend instead:
# VITE_PROXY_TARGET=http://localhost:4000
